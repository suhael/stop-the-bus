version: "3.8"
services:
  # The Redis Database
  redis:
    image: redis:alpine
    container_name: stb_redis
    ports:
      - "6379:6379"
    restart: always
    # Persistence: This saves your local room data even if you stop the container
    volumes:
      - redis_data:/data

volumes:
  redis_data:
