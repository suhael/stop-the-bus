{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "🚌 Launch Game Server",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceFolder}/apps/server/src/server.js",
      "restart": true,
      "envFile": "${workspaceFolder}/apps/server/.env",
      "preLaunchTask": "Start Redis" 
    }
  ]
}